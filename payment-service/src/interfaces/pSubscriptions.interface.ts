export enum SubscriptionType {
	MONTHLY = 'MONTHLY',
	YEARLY = 'YEARLY',
}
export enum SubscriptionStatus {
	ACTIVE = 'active',
	EXPIRED = 'expired',
	CANCELED = 'canceled',
}
export interface IPSubscription {}
